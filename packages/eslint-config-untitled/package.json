{
  "name": "eslint-config-untitled",
  "version": "0.0.1",
  "description": "TODO",
  "keywords": [
    "eslint",
    "eslintconfig",
    "eslint-config",
    "untitled"
  ],
  "author": "Will Allan <billyzkid@yahoo.com> (https://github.com/billyzkid)",
  "homepage": "https://billyzkid.github.io/untitled/",
  "bugs": "https://github.com/billyzkid/untitled/issues",
  "repository": "billyzkid/untitled",
  "license": "MIT",
  "main": "./build/base.js",
  "files": [
    "build"
  ],
  "engines": {
    "node": ">=6"
  },
  "peerDependencies": {
    "babel-eslint": "^7.1.1",
    "eslint": "^3.15.0",
    "eslint-plugin-untitled": "^0.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "eslint": "^3.15.0",
    "jest": "^18.1.0",
    "nodemon": "^1.11.0",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "build": "babel ./src --out-dir ./build --ignore *.test.js --source-maps",
    "build:watch": "npm run build -- --watch",
    "coverage": "npm run test -- --coverage",
    "coverage:watch": "npm run coverage -- --watch",
    "lint": "eslint ./src",
    "lint:fix": "npm run lint -- --fix",
    "lint:watch": "nodemon --watch ./src --exec npm run lint",
    "prebuild": "rimraf ./build",
    "precoverage": "rimraf ./coverage",
    "prepublish": "npm run lint && npm run test && npm run build",
    "test": "jest --config ../../.jestrc",
    "test:watch": "npm run test -- --watch"
  }
}
